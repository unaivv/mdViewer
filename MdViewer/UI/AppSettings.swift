import Foundation

/// `UserDefaults` keys for persisted viewer preferences (used with `@AppStorage`).
enum AppSettings {
    static let readerThemeKey = "readerTheme"
    static let pageZoomKey = "pageZoom"
}
